import React from "react";
import { Divider as SUIRDivider } from "@infinitecsolutions/semantic-ui-react";

const Divider = props => {
  return <SUIRDivider {...props} />;
};

export default Divider;
